server:
  port: 7999
spring:
  application:
    name: gateway
  cloud:
    nacos:
      config:
        server-addr: 127.0.0.1:8848
        file-extension: yaml
        refresh:
          enabled: false
        group: gok-base-cloud
  profiles:
    active: dev